export enum EPagination {
  PAGE = 1,
  LIMIT = 10,
}

export enum EStatusResult {
  PASSED = 'Passed',
  FAILED = 'Failed',
}

export enum EReminderRepeat {
  TWO_DAYS = 2,
  FOUR_DAYS = 4,
  EIGHT_DAYS = 8,
  SIXTEEN_DAYS = 16,
  THIRTY_TWO_DAYS = 32,
}